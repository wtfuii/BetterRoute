@code {
    [CascadingParameter] public RouterState State { get; set; } = default!;
}

<h3>Post @State.GetParameter("postId") by user @State.GetParameter("userId")</h3>
<p>Both <code>userId</code> (captured by an ancestor) and <code>postId</code> (captured here) are visible.</p>
