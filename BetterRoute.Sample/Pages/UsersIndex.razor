@code {
    [Inject] private NavigationManager Nav { get; set; } = default!;
}

<h2>All users</h2>
<ul>
    <li><a href="users/42/profile"
           @onclick="@(() => Nav.NavigateTo("users/42/profile"))"
           @onclick:preventDefault="true">User 42 — profile</a></li>
    <li><a href="users/99/profile"
           @onclick="@(() => Nav.NavigateTo("users/99/profile"))"
           @onclick:preventDefault="true">User 99 — profile</a></li>
</ul>
